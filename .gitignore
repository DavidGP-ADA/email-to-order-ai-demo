# Secrets — never commit
.env
.env.*
*.key
*.pem
*credentials*.json
.mcp.json

# n8n exports that may carry credentials or instance IDs
*-with-credentials.json
*.backup.json

# OS / editor noise
.DS_Store
Thumbs.db
.vscode/
.idea/
node_modules/
